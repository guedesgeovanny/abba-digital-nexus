-- Verificar se existem registros na tabela profiles
SELECT id, email, full_name, role, status FROM profiles;

-- Testar exclusão direta (substitua pelo ID real do usuário que quer deletar)
-- DELETE FROM profiles WHERE id = 'ID_DO_USUARIO_AQUI';